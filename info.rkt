#lang info

(define version "0.1")
(define deps (list "sxml"
                   "html-parsing"
                   "roman-numeral"))
(define pkg-desc "generate biber by parsing conference website")
(define pkg-authors '(lihebi))
